<h1>{{project.name}}</h1>

<p>{{project.description}}</p>

<p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>

<p>${{project.needed_funding}} needed!</p>

<button type="button" class="btn btn-primary">Fund this Project</button>